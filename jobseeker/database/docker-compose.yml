version: '3'
services:
  db:
    image: postgres:latest
    container_name: db
    ports:
        - "5432:5432"

    environment:
        POSTGRES_USER: pmassaro
        POSTGRES_PASSWORD: password
        POSTGRES_DB: jobseeker